<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="springGit0206_ex.message.mapper.UpdateMapper">
	<update id="update">
		update guestbook set guest_name=#{guestName}, message=#{message} where message_id=#{messageId}
	</update>
</mapper>